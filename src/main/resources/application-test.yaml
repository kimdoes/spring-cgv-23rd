spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cgvdb?allowPublicKeyRetrieval=true&useSSL=false&characterEncoding=UTF-8
    username: root
    password: leseoul2143**
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  file:
    name: logs/app.log
  level:
    root: info
    com.ceos23: debug
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace

  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} [req:%X{requestId}] - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} [req:%X{requestId}] - %msg%n"


jwt:
  secret-key: YWRkaXRpb25hbHN1cHBseWRlcG90c3JlcXVpcmVkISE

concurrency:
  storeId: kimdoes
  secret-key: eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJraW1kb2VzIiwiaWF0IjoxNzc2MTM2OTQzfQ.XdRtUpJ7emITaJ5yZ0ENomQLmon1in6TsbpbHlrDSko